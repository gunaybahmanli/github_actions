name: "02 - Events"
on: 
    workflow_dispatch:
    push:
    pull_request:
    schedule:
      - cron: "*/5 * * * *"
jobs:
    event-info-job:
        runs-on: ubuntu-latest
        steps:
          - name: Print trigger event
            run: echo "My trigger is ${{ github.event_name }} event"
